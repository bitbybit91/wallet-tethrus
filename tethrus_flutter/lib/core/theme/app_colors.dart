import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Primary brand colors
  static const Color primary = Color(0xFF26A17B);
  static const Color primaryVariant = Color(0xFF1E8A67);
  static const Color secondary = Color(0xFF00D4AA);

  // Dark theme
  static const Color darkBackground = Color(0xFF0D0D1A);
  static const Color darkSurface = Color(0xFF1A1A2E);
  static const Color darkSurfaceVariant = Color(0xFF252542);

  // Light theme
  static const Color lightBackground = Color(0xFFF5F5FA);
  static const Color lightSurface = Color(0xFFFFFFFF);
  static const Color lightText = Color(0xFF1A1A2E);

  // Semantic colors
  static const Color error = Color(0xFFFF4757);
  static const Color buyAccent = Color(0xFF2ED573);
  static const Color sellAccent = Color(0xFFFF6348);
  static const Color warning = Color(0xFFFFD93D);
  static const Color info = Color(0xFF54A0FF);

  // Text colors
  static const Color textPrimary = Color(0xFFFFFFFF);
  static const Color textSecondary = Color(0xFF8E8EA0);
  static const Color textMuted = Color(0xFF5A5A72);

  // Border
  static const Color border = Color(0xFF2A2A45);

  // Splash screen
  static const Color splashBackground = Color(0xFF1A1A2E);

  // Online status
  static const Color online = Color(0xFF2ED573);
  static const Color offline = Color(0xFF5A5A72);

  // Trust level colors
  static const Color trustExcellent = Color(0xFF2ED573);
  static const Color trustHigh = Color(0xFF26A17B);
  static const Color trustAverage = Color(0xFFFFD93D);
  static const Color trustLow = Color(0xFFFF6348);
  static const Color trustVeryLow = Color(0xFFFF4757);
  static const Color trustUnproven = Color(0xFF5A5A72);
}
